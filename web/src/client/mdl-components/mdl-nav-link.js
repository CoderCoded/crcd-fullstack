// This tag uses crcd-icons, which will be a iconset created using IcoMoon
import tagHtml from './mdl-nav-link.html'

const tagAttrs = 'class="mdl-navigation__link { mdl-navigation__link--current: opts.isCurrent }"'

riot.tag('mdl-nav-link', tagHtml, tagAttrs, function (opts) {
})
